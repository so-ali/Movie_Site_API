# Movie
This project is meant to be simple test API for movie website.

# Requirement
* [Docker](https://docs.docker.com/get-docker/)

# Build
To see the full list of available commands, pass the `-docs` switch:
```
docker-compose --env-file app/.env  up --build -d 
```